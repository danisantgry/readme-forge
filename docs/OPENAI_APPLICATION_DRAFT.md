# Codex for Open Source Application Draft

Repository:

```text
https://github.com/danisantgry/readme-forge
```

Role:

```text
Primary maintainer
```

Why does this repository qualify?

```text
readme-forge helps open-source maintainers generate accurate README drafts from real project metadata. It supports offline generation, deterministic README badges, workspace summaries for monorepos, versioned repository configuration, a 60-second adoption kit with config and optional GitHub Actions scaffolding, README doctor reports with ecosystem-aware health and adoption diagnostics, a generated README gallery for real project shapes, profile-based README quality scoring, minimum score gates for CI, fixture-based regression coverage, --check quality reports, --diff review, JSON automation output, template presets, optional AI refinement, contribution/security docs, examples, releases, and an active roadmap.
```

How will you use API credits?

```text
I will use API credits to improve optional AI-assisted README refinement, test prompt quality across real project types, build maintainer automation for documentation reviews, triage issues, prepare release notes, and ensure generated docs stay accurate without storing secrets.
```

Anything else?

```text
I am actively building this as a maintainer-focused open-source tool. The project is public, MIT licensed, tested, released, and structured for outside contributions with issue templates, PR guidance, security policy, generated examples, roadmap issues, changelog, diff review, versioned config defaults, an init command for safe adoption, a doctor command for actionable documentation diagnostics, profile-based README quality checks, fixture-based regression tests, Actions-ready validation, and automation-friendly JSON output.
```
