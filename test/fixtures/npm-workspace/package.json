{
  "name": "fixture-npm-workspace",
  "description": "A fixture npm workspace.",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "test": "npm -ws test"
  }
}
